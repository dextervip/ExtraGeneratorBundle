{% set route_name_prefix =  bundle|lower|replace({'bundle':''})~'_'~ route_name_prefix %}

{% block extends %}
{{ "{% extends '::base.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block body -%}" }}
<div class="row">
    <div class="col-md-12">

        <h1>{{ entity }} creation</h1>

        {{ '{{ form_start(form, {"attr": {"novalidate": "novalidate"}}) }}' }}
        {{ '{{ form_widget(form) }}' }}
                <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-9">
                        <button type="submit" class="btn btn-primary"><i class="fa fa-floppy-o"></i> {{ '{{ "Save"|trans }}' }}
                        </button>
                    </div>
                </div>
        {{ '{{ form_end(form) }}' }}

        <hr/>

        {% set hide_edit, hide_delete = true, true %}
        {% include 'crud/views/others/record_actions.html.twig.twig' %}

    </div>
</div>
{{ "{% endblock %}" }}
{% endblock body %}
